package net.gommagomma.smfn.graphics.core;

/**
 * Interfaccia specifica per il rendering 1D (grafici a linee, scatter plot).
 */
public interface Renderer1D
extends Renderer
{
    // Metodi specifici per il rendering 1D possono essere aggiunti qui se necessario,
    // ad esempio per disegnare simboli di plot specifici (quadrati, cerchietti).
}
