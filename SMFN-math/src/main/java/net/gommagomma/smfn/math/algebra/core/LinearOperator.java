package net.gommagomma.smfn.math.algebra.core;

/**
 * Rappresenta un operatore lineare, ovvero un operatore che preserva la struttura 
 * algebrica (addizione e scalatura) tipica degli spazi vettoriali o dei moduli.
 *
 * @param <T> il tipo degli elementi su cui agisce l'operatore lineare
 */
public interface LinearOperator<T>
extends Operator<T>
{}
